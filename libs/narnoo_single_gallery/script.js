imagebox.creategallery('slg', 'Single Link Gallery', {
		// gallery options go here, these will affect only the images in this gallery
		galleryTitle: '%GALLERY%:  %LIST%'
	});
	
	// build imagebox
imagebox.build({
	// global options go here, these will affect all images
})